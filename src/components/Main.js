import React from 'react'
import { Route } from 'react-router-dom'
import Header from './Header'
import BookingForm from './BookingForm';

function Main({ availableTimes, occasion , bannerImg }) {
  return (
    <main>
      
    </main>
  )
}

export default Main